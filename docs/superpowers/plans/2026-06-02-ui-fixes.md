# UI Fixes (7 items) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Fix 7 UI/UX issues: PR/PB labels, category badge text, add-exercise first-record inline, time input h/m/s, 1RM picker scroll state, 1RM table sections, share button.

**Architecture:** All changes are isolated to existing files. No new files except the plan. No DB schema changes needed — `records_provider` already supports addRecord per exercise.

**Tech Stack:** Flutter/Dart, Riverpod, CupertinoWidgets

---

### Task 1: PR/PB label + Category badge text

**Files:**
- Modify: `lib/core/models/exercise.dart` — label getter
- Modify: `lib/features/home/exercise_card.dart` — `_daysSince()`
- Modify: `lib/features/home/add_exercise_sheet.dart` — `_categories` list
- Modify: `lib/features/home/category_drawer.dart` — may use label

- [ ] In `exercise.dart` change label: strength→`Weight`, running→`Run`, workout→`WOD`, custom→`Custom`
- [ ] In `add_exercise_sheet.dart` update `_categories` label strings to match
- [ ] In `exercise_card.dart` `_daysSince()`: strength shows `PR`, running/workout show `PB`

### Task 2: Add-exercise inline first record

**Files:**
- Modify: `lib/features/home/add_exercise_sheet.dart`
- Modify: `lib/providers/exercises_provider.dart` — add `addExerciseWithRecord`
- Modify: `lib/providers/records_provider.dart` — expose `addRecord` directly

- [ ] In `add_exercise_sheet.dart`: after category selection show dynamic first-record fields (same as RecordInputScreen fields per category)
- [ ] Single 추가 button saves exercise then record in one tap
- [ ] First record fields are optional (if blank, save exercise only)

### Task 3: Time input h/m/s

**Files:**
- Modify: `lib/features/record_input/record_input_screen.dart`

- [ ] Replace unit-selector + single field for running/workout with `TimeInputField` widget (already exists in `time_input_field.dart`)
- [ ] Wire `TimeInputField.onChanged` to `_durationSeconds` state variable
- [ ] Remove `_timeUnit`, `_timeController`; add `int _durationSeconds = 0`

### Task 4: 1RM Picker scroll state

**Files:**
- Modify: `lib/features/home/one_rm_panel.dart`

- [ ] Add `bool _scrolling = false` state
- [ ] Wrap `CupertinoPicker` in `NotificationListener<ScrollNotification>`; set `_scrolling=true` on start, `false` on end
- [ ] In picker children: show weight only when `!_scrolling`

### Task 5: 1RM table two sections

**Files:**
- Modify: `lib/features/home/one_rm_panel.dart` — `_OneRmTableScreen`

- [ ] Change from 2-column side-by-side layout to 2 sections stacked
- [ ] Section 1: 50%~85%, Section 2: 86%~120%
- [ ] Each section has a header row, then 2-column grid inside

### Task 6: Share button fix

**Files:**
- Modify: `lib/features/export/export_screen.dart`

- [ ] Add try/catch in `_share()` with `_showSnack` on error
- [ ] Add `sharePositionOrigin` for iPad (use RenderBox from context)
- [ ] Ensure `Share.shareXFiles` result is awaited properly
