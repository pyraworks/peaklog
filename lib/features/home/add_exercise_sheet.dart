import 'package:flutter/material.dart';

class AddExerciseSheet extends StatelessWidget {
  const AddExerciseSheet({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox(
    height: 100,
    child: Center(child: Text('운동 추가 (곧 구현)')),
  );
}
